# Private / company skills — do not share
skills/projects/

# OS files
.DS_Store

# Skill installer state (managed externally)
.skill-lock.json

.ruff_cache
skills/operations/clickup/scripts/__pycache__/

# Local secrets — real values never committed; .env.sample is the template.
.env
